All the create table sql command is compiled in CreateTables.txt, but doesn't seem to be working of all tables creation sql are ran at once

SO

To Create all the tables using only one script, start the script QuickSetup.txt

OR

Run start followed by the other scripts following the number